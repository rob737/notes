-- 

INTENT(What&Why)       		PATTERNS(How)
INTERFACES          ADAPTER(Y), FACADE(Y), COMPOSITE(Y), BRIDGE(Y)
RESPONSIBILITY 		SINGLETON(Y), OBSERVER(Y), MEDIATOR(Y), PROXY(Y), CHAIN OF RESPONSIBILITY(Y), FLYWEIGHT(Y)
CONSTRUCTION 		BUILDER(Y), FACTORY METHOD(Y), ABSTRACT FACTORY(Y), PROTOTYPE(Y), MEMENTO(Y)
OPERATIONS 			TEMPLATE METHOD(Y), STATE(Y), STRATEGY(Y), COMMAND(Y), INTERPRETER
EXTENSIONS 			DECORATOR(Y), ITERATOR(Y), VISITOR(Y)



continue : page 28.

Note : proper notes and implementation will be present in design_pattern project in C://projects.